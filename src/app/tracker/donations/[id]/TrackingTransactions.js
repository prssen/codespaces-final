import { useState } from 'react';
import ListViewTable from '@/app/accounting/list/ListViewTable';

const TrackingTransactions = () => {
    // 1) Prepare data in format required by table (1.2) move to hook, 1.4) call in main page when done with dev)
    
    // 3) Render table, 2) in drawer 
    return (
        {/* Title (left), Sort model (right), table underneath */}
    );
}

export default TrackingTransactions;